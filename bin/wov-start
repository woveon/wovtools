#!/usr/bin/env bash
  


docker run -it --rm \
  -v ${HOME}/.gitconfig:/root/.gitconfig:ro \
  -v ${HOME}/.aws:/root/.aws:ro \
  -v ${HOME}/.ssh:/root/.ssh:ro \
         wovtools/wovbase:3 \
         /bin/bash
